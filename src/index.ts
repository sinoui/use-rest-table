import { SortInfo, PageInfo } from '@sinoui/use-rest-page-api';
import useRestTable from './useRestTable';
import { DataSource } from './types';

export default useRestTable;

export { SortInfo, PageInfo, DataSource };
